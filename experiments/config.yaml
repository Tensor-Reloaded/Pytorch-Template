defaults:
  - common: default
  - dataset: cifar10
  - initializer: default
  - loss: crossentropyloss
  - model: PreResNet56
  - optimizer: sgd
  - scheduler: milestones
  - specific: baseline
  - dataset_model_specific: ${defaults.1.dataset}/${defaults.7.specific}/${defaults.4.model}
    optional: true

model_command: ${model.name}(${model.depth})
save_dir: "${model_command}/${aux_save_dir}/${seed}"

load_model: ""