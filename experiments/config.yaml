defaults:
  - hydra/launcher: basic
  - hydra/sweeper: basic   # nevergrad | basic
  - common: default
  - dataset: cifar10 #cifar10 #ImageNet2012
  - hyperparameters:  # leave empty for basic sweeper
    optional: true
  - initializer: default
  - loss: bcewithlogits
  - metrics: default
  - model: PreResNet56
  - optimizer: radam
  - scheduler: milestones
  - specific: baseline
  - transformations: cifar10
  - dataset_model_specific: ${defaults.1.dataset}/${defaults.9.specific}/${defaults.6.model}
    optional: true

storage_dir: "\\\\CRISTIAN-HOME\\Data"
save_dir: "${model.name}/${aux_save_dir}/${seed}"

load_model: ""

infer_only: False