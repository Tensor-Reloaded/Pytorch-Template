# @package optimizer

name: "SGD"
parameters:
  lr: 0.01
  weight_decay: 0.00001
  momentum: 0.9
  nesterov: True

max_norm: 1.0
grad_penalty: 0.0

batch_replay: 10

use_lookahead: True
lookahead_k: 8
lookahead_alpha: 0.5

use_SAM: False
SAM_rho: 0.05