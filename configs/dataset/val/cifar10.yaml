# @package dataset

name: "CIFAR-10"

load_params:
  root: "${storage_dir}"
  train: false
  download: true
save_in_memory: true

shuffle: false
batch_size: 512
num_workers: 4
pin_memory: true
drop_last: false

subset: 0.0


transform: val/cifar10

